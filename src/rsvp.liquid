---
layout: base.liquid
titleKey: titleRsvp
---

{%- comment -%}
  Stage 7. The Google Form is the one external origin this site loads
  (CLAUDE.md "The Google Form is the one external thing"). Its questions are
  Google's and are never restyled or scripted from here — only the frame
  around it, and the copy either side of it, are ours.
{%- endcomment -%}

{%- assign theme = themes[site.partyTheme] -%}

<h1>{{ theme.titleRsvp }}</h1>

<h2>{{ theme.calloutTitle }}</h2>
<p>{{ theme.calloutBody }}</p>

<div class="form-embed">
  <iframe src="https://forms.gle/3fsynXeHFPaYjCmX6" title="RSVP form"></iframe>
</div>

<p class="form-fallback">
  Form not loading?
  <a href="https://forms.gle/3fsynXeHFPaYjCmX6">Open it directly</a>.
</p>
